name: 🔁 Sync Org to Personal Repo (Fresh Push)

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout org repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for proper sync

      - name: 🔧 Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: 🔍 Debug GH_PAT presence
        run: echo "GH_PAT is ${{ secrets.GH_PAT && 'set' || 'not set' }}"

      - name: 🚀 Push to personal repo (Benjy0812/Gooble)
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          # Verify PAT is available
          if [ -z "$GH_PAT" ]; then
            echo "🚨 GH_PAT secret is not set!"
            exit 1
          fi
          
          # Debug: Show first few chars of PAT (safely)
          echo "PAT starts with: ${GH_PAT:0:4}..."
          
          # Disable git credential helper to force URL-based auth
          git config --global --unset-all credential.helper || true
          git config --global credential.helper ""
          
          # Ensure we're on main branch
          git checkout -B main
          
          # Push directly with PAT in URL (retry logic)
          for i in 1 2 3; do
            echo "Attempt $i to push..."
            if git push "https://${GH_PAT}@github.com/Benjy0812/Gooble.git" main --force; then
              echo "✅ Successfully pushed to personal repo!"
              break
            else
              echo "❌ Push failed on attempt $i"
              if [ $i -eq 3 ]; then
                echo "🚨 All push attempts failed!"
                exit 1
              fi
              echo "⏳ Retrying in 5 seconds..."
              sleep 5
            fi
          done